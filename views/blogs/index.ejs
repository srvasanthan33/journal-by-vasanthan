<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>Vasanthan</title>
</head>
<body>
    <div class="container" style="width: clamp(auto,50%,1200px)">
        <h1 class="mb-4">blogs</h1>
        <a href="blogs/new" class="btn btn-dark">new article</a>

        <% blogs.forEach( blog => { %>
            <div class="card mt-4">
               <div class="card-body">
                    <h4 class="card-title">
                        <%= blog.title %>
                    </h4>
                    <div class="card-subtitle text-muted mb-2">
                        <%= blog.createdAt.toLocaleDateString('en-GB') %>
                        <span class="lead"> <b>| <%= blog.category %> </b></span>
                    </div>
                    <div class="card-text mb-2">
                        <%= blog.description %>
                    </div>

               </div>
            </div>

        <% }) %>
    </div>
</body>
</html>