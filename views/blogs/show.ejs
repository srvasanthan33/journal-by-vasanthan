<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <title>blog title</title>
    <style>
        body{
            color:white;
            background-color: rgb(27, 26, 26);
        }

        .heading{
            
            display:flex;
            justify-content: space-between;
            align-items: center;

        }
    </style>
</head>
<body>
    <div class="container">
        <a onclick="window.history.back()" class="btn btn-outline-light mt-2"><< Go Back</a>
        <div class="heading">
            <div class="left-heading">
                <h1 class="mb-1"><%= blog.title %></h1>
                <div class="c"><%= blog.createdAt.toLocaleDateString() %></div>
            </div>
            <div class="right-heading">
                <h4 > <%= blog.category%></h4>
            </div>
        </div>
        <a href="/" class ="btn btn-light">All blogs</a>
        <a href="/blogs/edit/<%= blog.id %>" class ="btn btn-secondary">Edit</a>
        <div class="line mt-3" style="border-bottom: 1px solid rgb(193, 193, 193);"></div>

        <div class="article">
            <%= blog.markdown %>
        </div>
    </div>

    
</body>
</html>